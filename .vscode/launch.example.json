{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Flask",
            "type": "debugpy",
            "request": "launch",
            "module": "gunicorn",
            "args": [
                "--bind", "0.0.0.0:5000",
                "--reload",
                "run:app"
            ],
            "jinja": true,
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}/caten_music"
            }
        }
    ],
    "compounds": [
        {
            "name": "Flask",
            "configurations": [
                "Flask"
            ]
        }
    ]
}
